function calcularPromedio(nota1, nota2) {
    const promedio = (nota1 + nota2) / 2
    return promedio
}

const nota1 = 7
const nota2 = 5
const promediofinal = calcularPromedio(nota1, nota2);
console.log(promediofinal);
console.log(`La nota final de cursada es ${promediofinal}`);


